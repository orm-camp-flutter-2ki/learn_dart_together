class Point {
  double xPoint; // x 좌표
  double yPoint; // y 좌표

  Point({required this.xPoint, required this.yPoint});

  // named constructor
  // 값이 초기화되어 있어야 사용 가능하다.
  // Point.name()
  //     : x = xPoint,
  //       y = yPoint;
}
